# Sections 7-8 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement tagging/breakdown and reports/analytics for MasterScript.

**Architecture:** Add a `tagging` state slice to the existing `ScriptProject` object and keep all derived breakdown/report calculations in focused TypeScript library modules. React wiring adds top-level workspace tabs without introducing a second state store.

**Tech Stack:** React, TypeScript, Vite, Electron, pdf-lib, Vitest, existing CSS.

---

### Task 1: Section 7 Tagging Model And Helpers

**Files:**
- Modify: `src/types/screenplay.ts`
- Modify: `src/lib/screenplay.ts`
- Create: `src/lib/taggingBreakdown.ts`
- Create: `src/lib/taggingBreakdown.test.ts`

- [ ] Write tests for inline tags, catalog grouping, breakdown sheets, CSV export, and local auto-tag suggestions.
- [ ] Run `npm test -- src/lib/taggingBreakdown.test.ts` and confirm the missing-module failure.
- [ ] Add `DepartmentTagCategory`, `TaggedScriptRange`, `TagCatalogItem`, `TaggingState`, and `ScriptProject.tagging`.
- [ ] Implement helper functions: `ensureTaggingState`, `tagScriptSelection`, `buildTagCatalog`, `updateTagCatalogItem`, `buildBreakdownSheet`, `buildBreakdownCsv`, `autoTagScript`.
- [ ] Re-run focused tests.

### Task 2: Section 7 UI And Export Wiring

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/index.css`

- [ ] Add a `breakdown` workspace tab and rail icon.
- [ ] Hydrate old projects with fallback tagging state.
- [ ] Add controls for selected text phrase tagging, category selection, auto-tag suggestions, catalog metadata editing, scene breakdown, CSV export, and PDF export through existing `exportTextReportToPdf`.
- [ ] Run `npm test; if ($LASTEXITCODE -eq 0) { npm run build:web }`.

### Task 3: Section 8 Reports And Analytics Helpers

**Files:**
- Create: `src/lib/reportsAnalytics.ts`
- Create: `src/lib/reportsAnalytics.test.ts`

- [ ] Write tests for scene, character, location, department, dialogue, summary, and analytics dashboard data.
- [ ] Run `npm test -- src/lib/reportsAnalytics.test.ts` and confirm the missing-module failure.
- [ ] Implement report builders that return rows plus CSV text for in-app display and export.
- [ ] Implement analytics data for INT/EXT, Day/Night, dialogue/action ratio, and scene length histogram.
- [ ] Re-run focused tests.

### Task 4: Section 8 UI And Export Wiring

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/index.css`

- [ ] Add a `reports` workspace tab and rail icon.
- [ ] Render report selectors, tables, CSV/PDF export buttons, and lightweight SVG charts.
- [ ] Run `npm test; if ($LASTEXITCODE -eq 0) { npm run build:web }`.
- [ ] Rebuild the desktop package with `npm run build:desktop` so the downloadable PC app matches the web app.

### Coverage Notes

- Section 7 maps to a functional local tagging system, metadata catalog, breakdown sheet, printable/PDF/CSV exports, and rule-based suggestions.
- Section 8 maps to generated in-app report tables, CSV/PDF exports, and lightweight SVG analytics. Excel `.xlsx` export is not in this section of the user’s current request; CSV is implemented here and Excel remains part of the later import/export completeness section if needed.
