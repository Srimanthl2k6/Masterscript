# MasterScript Sections 4-6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the next three roadmap sections: character tools, writing productivity, and production tools.

**Architecture:** Extend the existing `ScriptProject` object with backward-compatible `characters`, `productivity`, and production sub-state. Keep calculations in focused TypeScript library modules with Vitest coverage, then wire the current React panels without introducing another state store.

**Tech Stack:** React 19, TypeScript, Vite, Electron, Vitest, existing CSS, existing `pdf-lib` for generated PDFs, Web Speech API for TTS.

---

### Task 1: Character Tools

**Files:**
- Modify: `src/types/screenplay.ts`
- Modify: `src/lib/screenplay.ts`
- Create: `src/lib/characterTools.ts`
- Create: `src/lib/characterTools.test.ts`
- Modify: `src/App.tsx`
- Modify: `src/index.css`

- [ ] Add profile, relationship, arc, and stats data structures.
- [ ] Write failing tests for profile updates, scene/page stats, dialogue distribution, relationship map edges, arc stages, and global rename through catalog/script data.
- [ ] Implement helpers and wire the Catalog tab into a character workspace with profile fields, stats, chart SVG, relationship list, and arc controls.
- [ ] Run `npm test && npm run build:web`.

### Task 2: Writing Productivity

**Files:**
- Modify: `src/types/screenplay.ts`
- Modify: `src/lib/screenplay.ts`
- Create: `src/lib/productivity.ts`
- Create: `src/lib/productivity.test.ts`
- Modify: `src/App.tsx`
- Modify: `src/index.css`

- [ ] Add focus/typewriter/fullscreen flags, goals, sprint sessions, streaks, and TTS voice assignments.
- [ ] Write failing tests for goal progress, sprint logging, streak calculation, read-through queue, and voice assignment.
- [ ] Implement helpers and add a Productivity tab plus draft-mode controls for focus/typewriter/fullscreen and TTS.
- [ ] Run `npm test && npm run build:web`.

### Task 3: Production Tools

**Files:**
- Modify: `src/types/screenplay.ts`
- Modify: `src/lib/screenplay.ts`
- Create: `src/lib/productionTools.ts`
- Create: `src/lib/productionTools.test.ts`
- Modify: `src/App.tsx`
- Modify: `src/index.css`

- [ ] Add stripboard, DOOD, call sheet, sides, shot list, and storyboard export data helpers.
- [ ] Write failing tests for stripboard generation/reorder, DOOD markers, call sheet content, sides content, shot CSV/PDF-ready reports, and storyboard export pages.
- [ ] Implement helpers and wire Production/Storyboards controls for exports and shot list editing.
- [ ] Run `npm test && npm run build:web`.
